# Crafter LGRL port — Python 3.13, NVIDIA GPU with CUDA 12.4.
#
# Install torch first, separately, from the CUDA index (same as the
# MiniGrid project):
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# then:
#   pip install -r crafter/requirements.txt

torch-ac==1.4.0
crafter==1.8.3
gym==0.26.2            # crafter's native API (wrapped to Gymnasium internally)
gymnasium>=0.29
numpy>=1.24
pyyaml>=6.0
matplotlib>=3.7
requests>=2.31        # only for the optional LLM planner (Ollama)

# Optional, for the full-framework (LLM planner) condition:
#   install Ollama (https://ollama.com/download) and run:
#   ollama pull qwen2.5:7b
